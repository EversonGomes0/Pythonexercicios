def area(c,d):
    terreno = c * d
    print(f'A área do terreno {a}x{b} é de {terreno}M2')
#Calculando a área de um terreno usando def

a = float(input('Digite a largura do terreno retângular: '))
b = float(input('Digite o comprimento do terreno: '))
area(a,b)
